# configs/

모델 하이퍼파라미터, threshold 값, 경로 설정 등을 코드와 분리해 관리하기 위한 폴더입니다. 1학기 종료 시점에는 이 저장소에 설정 파일이 커밋되어 있지 않습니다.

2학기에 실제 코드를 이관할 때, [`../docs/thresholds.md`](../docs/thresholds.md)에 정리된 종목별 threshold 값들을 하드코딩 대신 이 폴더의 설정 파일(예: `thresholds_squat.yaml`)로 분리하는 것을 권장합니다.
